import type {Suggestion} from '../core/suggestions/engine'
export function SuggestionCard({suggestion,onPreview,onAdd,playing}:{suggestion:Suggestion;onPreview:()=>void;onAdd:()=>void;playing:boolean}){return <article className="suggestion-card"><div className="suggestion-top"><span className="route-tag">{suggestion.category}</span><span className="feeling">{suggestion.feeling}</span></div><h3>{suggestion.title}</h3><div className="path">{suggestion.path.map((c,i)=><span key={c+i}>{c}{i<suggestion.path.length-1&&<b>→</b>}</span>)}</div><p>{suggestion.explanation}</p><details><summary>Why this fits</summary><ul>{suggestion.evidence.map(e=><li key={e}>{e}</li>)}</ul></details><div className="card-actions"><button className="button ghost" onClick={onPreview}>{playing?'■ Stop':'▶ Preview'}</button><button className="button add" onClick={onAdd}>Add path <span>＋</span></button></div></article>}
