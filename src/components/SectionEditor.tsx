import {useState} from 'react'
import {sectionKinds,type SectionDefinition,type SectionKind} from '../core/song/sections'

export function SectionEditor({section,onSave,onCancel}:{section:SectionDefinition;onSave:(kind:SectionKind,name?:string)=>void;onCancel:()=>void}){const [kind,setKind]=useState(section.kind),[name,setName]=useState(section.name??'');return <section className="section-editor"><div><span className="eyebrow">Section settings</span><h3>Name the role this part plays</h3></div><label>Section type<select value={kind} onChange={event=>setKind(event.target.value as SectionKind)}>{sectionKinds.map(item=><option value={item.id} key={item.id}>{item.label}</option>)}</select></label><label>Custom label <small>optional</small><input value={name} onChange={event=>setName(event.target.value)} placeholder={sectionKinds.find(item=>item.id===kind)?.label}/></label><div><button className="button ghost" onClick={onCancel}>Cancel</button><button className="button primary" onClick={()=>onSave(kind,name.trim()||undefined)}>Save section</button></div></section>}
